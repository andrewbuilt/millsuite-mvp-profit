// Deprecated: the nav moved to components/side-nav.tsx (a left slide-out
// drawer, rendered once in app/(app)/layout.tsx). This stub keeps the ~20
// per-page `<Nav/>` imports compiling until they're removed in the cleanup
// chunk; it renders nothing, so there's no double nav.
export default function Nav() {
  return null
}
